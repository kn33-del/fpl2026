# Force Gradle to only use 8 parallel execution threads to match the AWS instance
org.gradle.workers.max=8

# Optional: Enable caching and parallel execution to speed up your local runs
org.gradle.parallel=true
org.gradle.caching=true